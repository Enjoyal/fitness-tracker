// Empty config: this project uses plain CSS (no PostCSS plugins).
// It exists to stop PostCSS/Vite from searching parent directories and picking
// up an unrelated config.
export default {
  plugins: {},
}
